(()=>{const dialog=document.querySelector('#walkthrough-dialog');const video=dialog.querySelector('video');document.querySelectorAll('[data-open-demo]').forEach(button=>button.addEventListener('click',event=>{event.preventDefault();button.closest('.mobile-menu')?.removeAttribute('open');dialog.showModal()}));dialog.querySelector('[data-close-demo]').addEventListener('click',()=>dialog.close());dialog.addEventListener('close',()=>video.pause());dialog.addEventListener('click',event=>{if(event.target===dialog){const r=dialog.getBoundingClientRect();if(event.clientX<r.left||event.clientX>r.right||event.clientY<r.top||event.clientY>r.bottom)dialog.close()}})})();
